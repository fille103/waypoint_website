<%= link_to("<< Back to List", {:action => 'index', :team_member_id => @team_member.id}, :class => 'back-link') %>

<div class="bios delete">
    <h2>Confirm Deletion</h2>

    <table summary="Bio detail view">
        <tr>
            <th>ID</th>
            <td><%= @bio.id %></td>
        </tr>
        <tr>
            <th>Biography</th>
            <td><%= @bio.biography %></td>
        </tr>
        <tr>
            <th>Team Member ID</th>
            <td><%= @bio.team_member_id %></td>
        </tr>
    </table>

    <%= form_for(:bio, :url => {:action => 'destroy', :id => @bio.id, :team_member_id => @team_member.id}) do |f| %>

    <div class="form-buttons">
        <%= submit_tag("Delete Biography") %>
    </div>

    <% end %>

</div>
